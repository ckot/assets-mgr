import { mediaProcessorPlugins } from './media-processor-plugins';

describe('mediaProcessorPlugins', () => {
  it('should work', () => {
    expect(mediaProcessorPlugins()).toEqual('media-processor-plugins');
  });
});
