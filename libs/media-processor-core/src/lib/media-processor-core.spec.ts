import { mediaProcessorCore } from './media-processor-core';

describe('mediaProcessorCore', () => {
  it('should work', () => {
    expect(mediaProcessorCore()).toEqual('media-processor-core');
  });
});
